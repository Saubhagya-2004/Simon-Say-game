let gameseq=[];
let user=[];
let started=false;
let level=0;
document.addEventListener('keypress',function(){
    if(started==false){
        alert('gane is started')
        started=true;
    }
    else{
        
    }
})